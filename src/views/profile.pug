extends layouts/main-layout

block variables
  - var title = 'My Profile';

block layout-content
  div Logged In User's Profile Page Here
    div
      if (userInfo)
        ul
          li #{userInfo.username}
        ul
          li #{userInfo.email}